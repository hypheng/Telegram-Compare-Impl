# iOS Host Shell Placeholder

这里保留 KMP 方案的 iOS host shell 位置。

当前只补到 Android-first 的 bootstrap 骨架，因此还没有真实 Xcode 工程。

补齐 iOS 壳之前，先满足:

1. 至少有一个切片在 `shared-domain`、`shared-data`、`androidApp` 跑通
2. AI delivery log 已能稳定记录 KMP 执行过程
3. acceptance report 和 parity matrix 已有对应证据
