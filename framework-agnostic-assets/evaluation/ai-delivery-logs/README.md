# AI Delivery Logs

这里存放每次有实质产出的 AI 执行记录。

记录目标:

- 追踪 AI 实际花了多少时间和 token
- 记录哪些问题影响了开发顺畅度
- 区分问题属于 `common`、`KMP` 还是 `CJMP`
- 为后续分析 AI 能力缺陷提供原始样本

建议:

- 每次执行一个文件
- 文件名使用 `YYYY-MM-DD-<slice>-<scope>-<task>.md`
- 对应切片的 acceptance report 和 parity matrix 都要能追到最新日志
